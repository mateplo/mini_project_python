# Copier ce fichier en .env et remplir les valeurs
API_KEY=                        # cle d'acces a l'API (choisir une valeur)
API_BASE=http://api:8000        # URL de l'API vue par le dashboard (Docker)
